server:
  port: 8077
spring:
  application:
    name: consumer-demo
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
